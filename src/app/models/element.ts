export class Element {
    constructor(
        public id: string,
        public name: string,
        public unit: string,
        public code: string,
        public sortOrder: number,
    ) {
    }
}
